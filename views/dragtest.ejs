<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />  <meta name="description" content="<%= __("invite") %>" />
    <link href="/javascripts/vendor/bootstrap/less/bootstrap.css" rel="stylesheet">
    <link rel='stylesheet' href='/stylesheets/main.css' />
    <title><%= __("Title") %></title>
    <script type="text/javascript" src="/javascripts/vendor/requirejs/require.js"></script>
    <script type="text/javascript" src="/javascripts/require-config.js"></script>
    <script type="text/javascript">
        require(["domReady","imageCollection","jquery"], function(domReady, ImageCollection, $){
            var imageCollection1 = new ImageCollection();
            $(".dd-main").append(imageCollection1.view.render().$el);
            var imageCollection2 = new ImageCollection();
            $(".dd-main").append(imageCollection2.view.render().$el);
        });
    </script>
</head>
<body class="dd-main">
<h1>Drag & Drop image inside or between containers</h1>
</body>
</html>